// UIInjectionPatch intentionally removed.
// Host registration and script injection are handled in DistrictPolicyUISystem.OnCreate,
// which runs after the city is fully loaded — avoiding interference with GameManager.Initialize().
